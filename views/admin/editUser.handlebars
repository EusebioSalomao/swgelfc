<div class="container">
    <div class="row">
        <div class="col-4 offset-4 mt-4">
            <h2>Actualizar Usuário:</h2>
            <hr>
            {{#each erros}}
            <div class="alert alert-danger">{{texto}}</div>
            {{else}}
            {{/each}}
            <div class="card p-3">
                <div class="card-body">
                    <form action="/user/editUser" method="post">
                        {{#if user}}
                        <label for="username">Nome de Usuário:</label>
                        <input type="text" name="username" value="{{usuario.username}}" class="form-control" required>
                        <label for="telefone">Telefone:</label>
                        <input type="text" name="telefone" value="{{usuario.telefone}}" class="form-control">
                        <label for="categoria">Categoria:</label>
                        <input type="text" name="categoria" disabled value="{{usuario.categoria}}" class="form-control">
                       {{!--  <div class="accordion" id="accordion1">
                            <div class="accordion-item">
                                <div class="accordion-header" id="headingOne">

                                    <button class="btn mt-2 accordion-btn btn-sm mb-3 btn-secondary" type="button" data-bs-toggle="collapse"
                                        data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                                        Alterar a senha
                                    </button>
                                </div>
                                <div id="collapseOne" class="accordion-collapse collapse"
                                    aria-labelledby="headingOne" data-bs-parent="#accordion1">
                                    <div class="accordion-body">

                                        <label for="senha">Senha:</label>
                                        <input type="password" name="senha"
                                            placeholder="Senha" class="form-control" required>
                                        <label for="senha2">Repete a senha:</label>
                                        <input type="password" name="senha2" placeholder="Senha" class="form-control"
                                            required>
                                    </div>

                                </div>
                            </div>
                        </div> --}}
                        <input type="hidden" name="id" value="{{usuario._id}}">

                        <div class="text-center">
                            <button type="submit" class="btn btn-success mt-2 ">Actualizar</button>
                            <a href="/user/allusers" class="btn btn-warning mt-2">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
                        {{else}}
                        <h4>Faça login para Editar</h4>
                        {{/if}}